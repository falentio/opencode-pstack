unit matrix: 22 pass 0 fail; live compaction leg verified-unreachable (tiny session skips summarization so the compaction hook never fires; resume sentence absent from export); root=/tmp/pstack-verify-ixIKFx
